import React from 'react';import {ResponsiveContainer,AreaChart,Area,XAxis,YAxis,Tooltip,CartesianGrid} from 'recharts';import {hours,when} from '../lib/format';
export function PageHeader({title,subtitle,children}:{title:string,subtitle?:string,children?:React.ReactNode}){return <header className="pageHead"><div><h1 className="pageTitle">{title}</h1>{subtitle&&<div className="subtle">{subtitle}</div>}</div><div className="controls">{children}</div></header>}
export function Metric({label,value}:{label:string,value:any}){return <div className="metric"><div className="metricLabel">{label}</div><div className="metricValue">{value}</div></div>}
export function TrendChart({data}:{data:any[]}){return <div className="chartBox"><ResponsiveContainer width="100%" height="100%"><AreaChart data={data}><defs><linearGradient id="x" x1="0" y1="0" x2="0" y2="1"><stop offset="0" stopColor="var(--accent)" stopOpacity={.28}/><stop offset="1" stopColor="var(--accent)" stopOpacity={0}/></linearGradient></defs><CartesianGrid stroke="var(--border-subtle)" vertical={false}/><XAxis dataKey="day" tick={{fill:'var(--text-tertiary)',fontSize:10}}/><YAxis tickFormatter={(v)=>`${(v/3600000).toFixed(0)}h`} tick={{fill:'var(--text-tertiary)',fontSize:10}}/><Tooltip contentStyle={{background:'var(--surface-1)',border:'1px solid var(--border-subtle)',borderRadius:6}} formatter={(v:any)=>hours(Number(v))}/><Area type="monotone" dataKey="ms" stroke="var(--accent)" fill="url(#x)" strokeWidth={2}/></AreaChart></ResponsiveContainer></div>}
export function Artwork({src,alt=''}:{src?:string|null,alt?:string}){return src?<img className="art" src={src} alt={alt}/>:<div className="art"/>}
export function Loading(){return <div className="empty">Loading…</div>}
export function ErrorBox({message}:{message:string}){return <div className="callout">{message}</div>}
export function Empty({text='No data yet.'}:{text?:string}){return <div className="empty">{text}</div>}
